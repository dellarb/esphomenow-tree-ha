# Shared headers for ESP-NOW LR components (espnow_types.h, espnow_crypto.h/.cpp)
# No ESPHome config schema — this component exists only to provide shared headers
# and the crypto implementation. AUTO_LOAD from bridge/remote pulls it in.
